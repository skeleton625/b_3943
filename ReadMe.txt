백준 3943 문제 풀이 중입니다.

문제 풀이를 완료했습니다.

해당 문제에서 c++언어를 사용할 경우 cin 입력 함수가 느리기 때문에 정해진 제한 시간 내에 입력을 다 받아내지 못합니다.

그렇기 때문에 c++의 cin 입력 함수를 사용하기 보다 c언어의 scanf 입력 함수를 사용해 문제를 해결하는 것이 좋습니다.

다른 문제에서 이와 같이 시간 초과가 생길 경우 이 점을 유의해서 문제에 접근하는 것이 좋을 것 같습니다.

( 참고로 c++언어에서 cin의 경우 std::ios::sync_with_stdio(false); 를 사용하면 처리 속도가 더 빨라진다고 합니다.) 
